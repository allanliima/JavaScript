const nome = 'Alan';
let nomeAluno;//undefined = não aponta pra local nenhuma na memória
let sobreNomeAlun0 = null; // não aponta pra local nenhuma na memória
const aprovado = true; // Boolean = true || False (lógico)

console.log(typeof aprovado);

const a = [1,2]
const b = a;
console.log(a);

b.push(3);
console.log(a);